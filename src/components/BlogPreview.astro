---
const { title, published, excerpt, href } = Astro.props;
---
<div class="py-8 md:py-8 shadow-lg border-2 m-2 p-2">
  <h5 class="font-bold text-2xl">{title}</h5>
  <small>Posted @ {new Date(published).toLocaleDateString("en-GB")}</small>
  <p class="text-xl">{excerpt}</p>
  <a class="underline text-indigo-700" href={href}>Read</a>
</div>

